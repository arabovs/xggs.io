import { createMuiTheme } from "@material-ui/core"

export const indexTheme = createMuiTheme({
  typography: {
    fontFamily: ["Open Sans Condensed", "sans-serif"].join(","),
  },
})

export default indexTheme
