import { createMuiTheme } from "@material-ui/core"

export const pricingTheme = createMuiTheme({
  typography: {
    fontFamily: ["Goblin One", "sans-serif"].join(","),
  },
})

export default pricingTheme
